Shell script to create a react app with a homepage and a starter navbar. 

To run this script git and clone the repo.

cd into the bin 

```cd bin```

Change user permissions 

```chmod 755 navbar.sh```

run 

```./navbar.sh```